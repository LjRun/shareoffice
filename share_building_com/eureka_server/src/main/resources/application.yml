server:
  port: 8756 # 注册中心占用8756端口
eureka:
  instance:
    hostname: 192.168.19.198
    appname: eureka-server-master
client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/  #其实就是 http://localhost:8080/eureka/

